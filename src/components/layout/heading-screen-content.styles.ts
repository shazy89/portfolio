import { theme } from 'theme'
import { css } from '@emotion/css'

export const container = css`
  /* nav height 4rem */
  height: calc(100% - 4rem);
  display: flex;
  flex-direction: row;
  gap: 3.8rem;
`
